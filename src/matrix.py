from typing import Dict, Any

def load_or_build(locations, config: Dict[str, Any]):
    # TODO: load npz or compute travel time/distance matrix
    # return dict like {"time": time_matrix, "dist": dist_matrix}
    raise NotImplementedError