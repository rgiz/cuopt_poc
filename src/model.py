from typing import Dict, Any

def build_problem(locations, duties, drivers, matrices, config: Dict[str, Any]) -> Dict[str, Any]:
    # TODO: convert to cuOpt-compatible problem dict (vehicles, orders, time windows, etc.)
    raise NotImplementedError